/**
 * Interface for rendering the game board.
 */
public interface Renderer {
    /**
     * Renders the board according to our need.
     * @param board the board to be rendered.
     */
    public void renderBoard(Board board);
}
